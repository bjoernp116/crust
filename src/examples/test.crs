fun add_two(a: u32, b: u32) -> u32 {
    let result: u32 = a + b;
    let constant: u32 = 3 + 5;
    return constant;
}

fun main() { 
    let foo_value: u32 = add_two(3, 2, 1);
    exit(foo_value);
}
