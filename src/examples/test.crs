fun main() -> u16 { 
    let sum = add(3, 4);
    sum = sum + 1;

    let predicate = sum == 5;

    if (predicate) {
        return predicate;
    }

    return sum;
}

fun add(a: u16, b: u16) -> u16 {
    return a + b;
}
